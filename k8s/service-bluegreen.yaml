apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: LoadBalancer
  selector:
    app: myapp
    version: green   # Cambia de blue a green cuando la validación sea exitosa
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
